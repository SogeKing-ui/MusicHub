[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

# SPA fallback so deep links like /post/abc work after deploy.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
